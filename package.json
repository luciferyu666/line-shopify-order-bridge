{
  "name": "line-shopify-order-bridge",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "vercel dev",
    "test": "vitest run",
    "lint": "echo 'no lint configured'",
    "deploy": "echo 'vercel --prod'",
    "postinstall": "pnpm --filter ./frontend install"
  },
  "dependencies": {
    "@line/bot-sdk": "^8.4.0",
    "axios": "^1.6.0",
    "dotenv": "^16.3.1",
    "pg": "^8.10.0",
    "winston": "^3.10.0",
    "cheerio": "^1.0.0-rc.12"
  },
  "devDependencies": {
    "vitest": "^1.5.0"
  }
}
